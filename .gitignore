# Daml build output
.daml/

# Scala / sbt build output
target/
.bsp/
.bloop/
.metals/
project/project/
project/target/
.coursier-cache/

# IDE
.idea/

# Canton runtime logs
log/

# Vendored splice build outputs — generated by daml/nix/vendored.nix. The DARs and OpenAPI specs are
# committed (real files, so JitPack and fresh clones build with no nix); the derivation remains the
# refresh mechanism (nix build .#vendored-splice, then copy over the committed copies). Only the
# test-only external-test-sources tree stays generated/ignored. See the vendored READMEs.
daml/external-test-sources/splice-token-standard-v2-test/daml

# Nix / direnv
result
.direnv/

# Local direnv overrides
.envrc.local
